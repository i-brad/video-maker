"""
Code that more than one tool needs.

Anything in here is used by at least two of the tools in the folders alongside
this one. Everything else stays with the tool that owns it — a helper only one
tool calls is easier to follow sitting next to its caller.
"""
